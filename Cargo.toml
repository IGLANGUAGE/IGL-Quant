[package]
name = "igl-quant"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
wgpu = "0.20"  # Для работы с WebGPU из Rust

[profile.release]
lto = true
opt-level = 's'  # Оптимизация по размеру
